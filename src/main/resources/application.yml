spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    password: postgres
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
  flyway:
    url: jdbc:postgresql://localhost:5432/postgres
    schemas: restsample
    user: postgres
    password: postgres